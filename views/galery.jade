extends layout

block content
    h2.h1 galeria
    .row
    - if (pic.length > 1) {
        - for (var i = 9; i < pic.length; i++) {
            img.col-md-6.col-lg-3.col-sm-12(src="#{pic[i]}", alt="#{pic[i]}", style='padding-top:20px;')
        - }
    - }

        
        
    
